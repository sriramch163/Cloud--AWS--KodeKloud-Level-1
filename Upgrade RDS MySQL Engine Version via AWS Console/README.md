# Upgrade RDS MySQL Engine Version via AWS Console

## 🎯 Objective of the Task

The objective is to upgrade the existing RDS instance `xfusion-rds` in the `us-east-1` region from MySQL engine version 8.4.4 → 8.4.5.

This ensures the instance receives the latest performance improvements and security patches.

Your task is to:

- Locate the RDS instance `xfusion-rds`.
- Modify it to upgrade the MySQL engine version to 8.4.5.
- Apply the changes immediately.
- Wait until the instance returns to Available status after the upgrade.

This validates that you can perform version upgrades on production databases safely and correctly.

## 📚 About KodeKloud AWS Tasks

This task is part of the KodeKloud AWS hands-on learning series, designed to provide practical experience with AWS services. These tasks simulate real-world DevOps scenarios and help build proficiency in:

- AWS CLI operations
- EC2 instance management
- Security and access control
- Cloud infrastructure setup
- Best practices for AWS resource management

Each task includes step-by-step commands and verification screenshots to ensure successful completion.

## 📁 Directory Structure

```
Upgrade RDS MySQL Engine Version via AWS Console/
├── Steps/
│   ├── apply immediately.png
│   └── select engine.png
├── Task/
│   └── task.png
├── steps.txt
└── Verify.png
```