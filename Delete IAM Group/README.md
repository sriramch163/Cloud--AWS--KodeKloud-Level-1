# Delete IAM Group

## 🎯 Objective

As part of AWS environment cleanup, the DevOps team must remove IAM resources that are no longer required. An IAM group named `iamgroup_rose` is no longer needed and must be deleted to maintain a clean IAM structure.

The goal is:

Delete the IAM group named `iamgroup_rose` in the `us-east-1` region.

This removes obsolete IAM configurations from the AWS account.

## 📚 About KodeKloud AWS Tasks

This task is part of the KodeKloud AWS hands-on learning series, designed to provide practical experience with AWS services. These tasks simulate real-world DevOps scenarios and help build proficiency in:

- AWS CLI operations
- EC2 instance management
- Security and access control
- Cloud infrastructure setup
- Best practices for AWS resource management

Each task includes step-by-step commands and verification screenshots to ensure successful completion.

## 📁 Directory Structure

```
Delete IAM Group/
├── Steps/
│   ├── group delete.png
│   └── steps.txt
├── Task/
│   └── task.png
└── Verify.png
```
