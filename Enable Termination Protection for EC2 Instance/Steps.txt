🖥️ Steps to Complete the Task Using AWS Console (UI)

Follow these steps in the AWS Management Console:

✅ STEP 1 — Log In to AWS Console

Use the credentials provided:

Console URL

Username

Password

Region: us-east-1

✅ STEP 2 — Open EC2 Service

In the AWS search bar, type EC2

Click EC2 to open the EC2 Dashboard

✅ STEP 3 — Locate the Instance nautilus-ec2

In the left menu, click Instances → Instances

Find nautilus-ec2 in the instance list
(You can also filter by Instance Name)

✅ STEP 4 — Enable Termination Protection

Select the nautilus-ec2 instance

Click Actions

Go to:

Actions → Instance settings → Change termination protection


A dialog box will open

Turn ON the toggle/button for Enable termination protection

Click Save / Apply

🚀 STEP 5 — Verify Protection is Enabled

With the instance selected:

Scroll down to the Details tab

Look for:

Termination protection: Enabled


This confirms the task is successfully completed.

✅ Task Completed When

The instance nautilus-ec2 is still running (not terminated)

The Termination Protection value shows Enabled