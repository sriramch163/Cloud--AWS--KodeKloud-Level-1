# Enable Termination Protection for EC2 Instance

## 🎯 Objective

As part of the migration, an EC2 instance named `nautilus-ec2` was created in the `us-east-1` region. However, the team forgot to enable Termination Protection, which prevents accidental deletion of the instance.

The objective of this task is to:

Enable Termination Protection for the EC2 instance `nautilus-ec2` in the `us-east-1` region to ensure it cannot be accidentally terminated.

## 📚 About KodeKloud AWS Tasks

This task is part of the KodeKloud AWS hands-on learning series, designed to provide practical experience with AWS services. These tasks simulate real-world DevOps scenarios and help build proficiency in:

- AWS CLI operations
- EC2 instance management
- Security and access control
- Cloud infrastructure setup
- Best practices for AWS resource management

Each task includes step-by-step commands and verification screenshots to ensure successful completion.

## 📁 Directory Structure

```
Enable Termination Protection for EC2 Instance/
├── Steps/
│   ├── enable.png
│   └── verify.png
├── Task/
│   └── task.png
├── Steps.txt
└── README.md
```
